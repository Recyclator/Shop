let posProducts=[],cart=[];
async function loadPOSProducts(){try{const r=await fetch(`${API_URL}/pos/products?limit=100&estado=activo`,{headers:{'Authorization':'Bearer '+token}});if(r.ok){posProducts=(await r.json()).data||[];renderPOSProducts(posProducts);}}catch(e){}}
function renderPOSProducts(p){const g=document.getElementById('pos-products-grid');if(!p.length){g.innerHTML='<div style="text-align:center;padding:40px;color:var(--text-mut);grid-column:1/-1;">Sin productos</div>';return;}g.innerHTML=p.map(x=>`<div class="card" style="cursor:pointer;padding:12px;" onclick="addToCart(${x.id})"><div style="font-weight:500;font-size:13px;margin-bottom:4px;">${x.nombre}</div><div class="mono" style="font-size:14px;color:var(--accent);">$${(x.precio||0).toLocaleString('es-CO')}</div><div style="font-size:11px;color:var(--text-mut);">Stock: ${x.stock||0}</div></div>`).join('');}
function searchPOSProducts(){const q=document.getElementById('pos-search').value.toLowerCase();renderPOSProducts(posProducts.filter(p=>p.nombre.toLowerCase().includes(q)||(p.sku||'').toLowerCase().includes(q)));}
function addToCart(id){const p=posProducts.find(x=>x.id===id);if(!p)return;const e=cart.find(c=>c.id===id);if(e){e.qty++;}else{cart.push({...p,qty:1});}renderCart();}
function removeFromCart(id){cart=cart.filter(c=>c.id!==id);renderCart();}
function updateCartQty(id,delta){const e=cart.find(c=>c.id===id);if(!e)return;e.qty+=delta;if(e.qty<=0)cart=cart.filter(c=>c.id!==id);renderCart();}
function renderCart(){document.getElementById('cart-count').textContent=`(${cart.reduce((s,c)=>s+c.qty,0)})`;const c=document.getElementById('cart-items');if(!cart.length){c.innerHTML='<div style="text-align:center;padding:40px;color:var(--text-mut)">Vacío</div>';document.getElementById('cart-subtotal').textContent='$0';document.getElementById('cart-total').textContent='$0';return;}const t=cart.reduce((s,x)=>s+(x.precio||0)*x.qty,0);c.innerHTML=cart.map(x=>'<div style="display:flex;justify-content:space-between;align-items:center;padding:8px 0;border-bottom:1px solid var(--border);">'+
'<div><div style="font-weight:500;">'+x.nombre+'</div><div style="font-size:12px;color:var(--text-mut);">$'+(x.precio||0).toLocaleString('es-CO')+' c/u</div></div>'+
'<div style="display:flex;align-items:center;gap:6px;">'+
'<button class="btn btn-sm btn-outline" onclick="updateCartQty('+x.id+',-1)" style="width:28px;height:28px;padding:0;line-height:1;">'+"\u2212"+'</button>'+
'<span class="mono" style="min-width:24px;text-align:center;">'+x.qty+'</span>'+
'<button class="btn btn-sm btn-outline" onclick="updateCartQty('+x.id+',1)" style="width:28px;height:28px;padding:0;line-height:1;">+</button>'+
'<span class="mono" style="margin-left:8px;">$'+((x.precio||0)*x.qty).toLocaleString('es-CO')+'</span>'+
'<button class="btn btn-sm btn-danger" onclick="removeFromCart('+x.id+')" style="margin-left:4px;">'+"\u00D7"+'</button>'+
'</div></div>').join('');document.getElementById('cart-subtotal').textContent='$'+t.toLocaleString('es-CO');document.getElementById('cart-total').textContent='$'+t.toLocaleString('es-CO');}
async function completePOSSale(m){if(!cart.length){Toast.warning('Vacío','Agrega productos');return;}const items=cart.map(c=>({producto_id:c.id,cantidad:c.qty,precio_unitario:c.precio})),t=cart.reduce((s,c)=>s+(c.precio||0)*c.qty,0);try{const r=await fetch(`${API_URL}/orders/pos`,{method:'POST',headers:{'Authorization':'Bearer '+token,'Content-Type':'application/json'},body:JSON.stringify({items,metodo_pago:m,total:t})});if(r.ok){Toast.success('Venta','$'+t.toLocaleString('es-CO'));cart=[];renderCart();}else{Toast.error('Error',(await r.json()).error?.message||'Error');}}catch(e){Toast.error('Error','Error');}}
async function loadPOSSales(){try{const r=await fetch(`${API_URL}/orders?page=1&limit=20`,{headers:{'Authorization':'Bearer '+token}});if(r.ok){const o=(await r.json()).data||[];document.getElementById('sales-tbody').innerHTML=o.length===0?'<tr><td colspan="5" style="text-align:center;padding:40px;color:var(--text-mut)">Sin ventas</tr>':o.map(x=>`<tr><td class="mono">#${x.id}</td><td>${new Date(x.created_at).toLocaleTimeString('es-CO',{hour:'2-digit',minute:'2-digit'})}</td><td>${x.items?.length||0} items</td><td class="mono">$${(x.total||0).toLocaleString('es-CO')}</td><td>${x.metodo_pago||'-'}</td></tr>`).join('');}}catch(e){}}
async function scanBarcode(){const c=document.getElementById('pos-barcode').value;if(!c)return;try{const r=await fetch(`${API_URL}/products/barcode/${c}`,{headers:{'Authorization':'Bearer '+token}});if(r.ok){const p=(await r.json()).data;addToCart(p.id);document.getElementById('pos-barcode').value='';}else{Toast.error('Error','No encontrado');}}catch(e){}}
document.addEventListener('DOMContentLoaded',()=>{loadPOSProducts();});